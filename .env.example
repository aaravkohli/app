# Backend Configuration

# Primary API key for main LLM operations (required)
GOOGLE_API_KEY=your_google_api_key_here

# Secondary API key for automatic prompt sanitization (optional but recommended)
# If not set, will fall back to GOOGLE_API_KEY
# Benefits of using a separate key:
#   - Separate rate limits and quota management
#   - Cost tracking for security operations
#   - Resource isolation between main and safety processing
GOOGLE_SAFETY_API_KEY=your_secondary_google_api_key_here

FLASK_ENV=production
PORT=5000
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:8080,http://localhost:8081
MAX_UPLOAD_SIZE=26214400

# Firebase Authentication
# Path to Firebase service account JSON file for token verification
# For production: Set this to the path or use environment variable content
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/serviceAccountKey.json

